{
  "build": {
    "env": {
      "PYTHONUNBUFFERED": "1"
    }
  },
  "deploy": {
    "env": {
      "PORT": "8000" // Or any other suitable port number, Railway will usually inject this automatically if not set
    },
    "startCommand": "uvicorn main:app --host=0.0.0.0 --port=${PORT}"
  }
}
